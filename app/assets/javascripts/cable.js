// Action Cable provides the framework to deal with WebSockets in Rails.
// You can generate new channels where WebSocket features live using the `rails generate channel` command.
//
//#### no action cable needed here = require action_cable
//#### no action cable needed here = require_self
//#### no action cable needed here = require_tree ./channels
//#### no action cable needed here  (function() {
//#### no action cable needed here    this.App || (this.App = {});
//#### no action cable needed here    App.cable = ActionCable.createConsumer();
//#### no action cable needed here  }).call(this);
